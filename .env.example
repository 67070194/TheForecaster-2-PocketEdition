SERVER_PORT=3001
BASE_TOPIC=TFCT_2_PE
MQTT_URL=wss://broker.hivemq.com:8884/mqtt
MQTT_USERNAME=
MQTT_PASSWORD=
DATABASE_URL=postgresql://postgres:postgres@db:5432/iotdb
API_PROXY_TARGET=http://server:3001
UPLOADS_DIR=/app/uploads
STATIC_DIR=/app/public

# Postgres baseline (used by backup service)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=iotdb

# Backup interval (seconds); default 86400 (1 day)
BACKUP_INTERVAL_SECONDS=86400

# Optional: Build-time API endpoints for web frontend
# Leave empty for Docker stack (nginx will proxy to server)
# Set for external deployments (e.g., GitHub Pages)
VITE_API_BASE=
VITE_FW_BASE=
